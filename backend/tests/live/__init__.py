"""Live-paper E2E suite — the Phase 2 deliverable proof.

Everything here talks to the REAL Alpaca paper API and runs the REAL engine
process. Deselected by default (`addopts = "-m 'not live_paper'"`), env-gated,
market-hours-gated, and never run in CI. See MANUAL-SETUP.md for the runbook.
"""
